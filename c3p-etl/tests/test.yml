# 
# Environment-specific configuration
#

# Type of the environment
environment: test

# Location of the data files saved to the file system
data_dir: /data/dev/${LOGNAME}

sqlite3:
  db_file: /tmp/test-${LOGNAME}.db
  prod_file: /data/prod/events/tennis.db

